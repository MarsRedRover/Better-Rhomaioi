my_give_test_trait_decision = {
	title = "Wild Lion Chase"
	desc = "A very Testy Test"
	sort_order = integer
	selection_tooltip = "this a test decision for a very modding inexperienced Lion"
	
	
	is_shown = {
		is_lowborn = no
		highest_held_title_tier >= 4
		NOT = {
		has_trait = masterbuilder
		}
		OR = {
		has_trait = education_stewardship_3
		has_trait = education_stewardship_4
		has_trait = education_stewardship_5
		}
	
	}
	cost = {
		gold = 200
		piety = 200
	}
	effect = {
		add_trait = masterbuilder
		give_nickname = nick_the_builder
		add_prestige = 500
	}
	confirm_text = "Bad Idea!"
}

Window_trigger_decision = {
	title = "Window Trigger"
	desc = "click to trigger custom window for gui tests"
	
	is_shown = {
		is_lowborn = no
		highest_held_title_tier >= 4
	}	
	effect = {
		add_prestige = 1
		trigger_event = decision_relevant_events.001
	}
	confirm_text = "Let it Window!"
}

